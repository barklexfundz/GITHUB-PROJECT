import "./App.css";
import Home from "./pages/Home";



//STEPS
// installations...
// 1)..create the different folders needed (components folders(card,followers,index,info,search,user), style folders(card,followers,info,search,user,app.css&index.css), hooks(useGlobalContext),pages(Home),Context folder(Data, folder(user.js,followers.js))
//2)..

function App() {
  return (
    <>
      <Home />
      
    </>
  );
}

export default App;
